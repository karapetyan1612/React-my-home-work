import CreatButton from "../CreatButton";

function LogoComp(params) {
  return (
    <div className="logocomp">
      <CreatButton className="logoInst"></CreatButton>
      <CreatButton className="logoFb"></CreatButton>
      <CreatButton className="logoYout"></CreatButton>
    </div>
  );
}
export default LogoComp;
